import { LoadingSpinner, LoadingSpinnerContainer } from "./SpinnerStyle";

const Spinner = () => {
  return (
    <LoadingSpinnerContainer>
      <LoadingSpinner></LoadingSpinner>
    </LoadingSpinnerContainer>
  );
};

export default Spinner;
